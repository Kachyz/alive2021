// Crea una variable que almacene el resultado de la operación  23 al cubo
//si no te corre quita el function:
(function variables() {
  // Tu código va aquí
  let operacion = 23*23*23;
  (console.log (operacion));
 
  let aver = Math.pow(23, 3); 
  (console.log (aver));
})()