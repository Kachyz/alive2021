// Guarda en la variable suma la suma de los tres números del arreglo numeros
let numeros = [12, 4, 8,12,7,15];

let suma = numeros[0]+numeros[1]+numeros[2]
console.log(suma)

numeros.forEach(elemento =>suma += elemento);
console.log(suma);

sume = sum(numeros)
console.log(sume)
// Inicio de tu código


// Fin de tu código

module.exports = suma;
