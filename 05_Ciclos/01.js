// Utiliza un ciclo para encontrar la suma de todos los numeros en el rango de 0 a 200

let resultado

// Inicio de tu código
for(let contador = 0; contador <= 200; contador += 1) {
  console.log(contador);
}
// Fin de tu código

module.exports = resultado;
